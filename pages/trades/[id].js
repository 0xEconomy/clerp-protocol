import { Header, Layout, Modal, Pagination, Table } from "../../components";

function Trade() {
	return (
		<Layout>
			<Header />
			<Table />
			<Pagination />
			<Modal />
		</Layout>
	);
}

export default Trade;