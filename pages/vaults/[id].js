import { Header, Layout, Modal, Pagination, Table } from "../../components";

function Vault() {
	return (
		<Layout>
			<Header />
			<Table />
			<Pagination />
			<Modal />
		</Layout>
	);
}

export default Vault;