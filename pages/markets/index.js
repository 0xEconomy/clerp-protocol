import { Header, Layout, Modal, Pagination, Table } from "../../components";

function Markets() {
	return (
		<Layout>
			<Header />
			<Table />
			<Pagination />
			<Modal />
		</Layout>
	);
}

export default Markets;