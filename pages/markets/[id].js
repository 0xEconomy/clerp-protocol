import { Header, Layout, Modal, Pagination, Table } from "../../components";

function Market() {
	return (
		<Layout>
			<Header />
			<Table />
			<Pagination />
			<Modal />
		</Layout>
	);
}

export default Market;